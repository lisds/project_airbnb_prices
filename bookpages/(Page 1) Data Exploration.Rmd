---
jupyter:
  jupytext:
    text_representation:
      extension: .Rmd
      format_name: rmarkdown
      format_version: '1.2'
      jupytext_version: 1.15.2
  kernelspec:
    display_name: Python 3 (ipykernel)
    language: python
    name: python3
---

```{python}
import pandas as pd
from pathlib import Path
```

```{python}
data_path = Path('data/proj_data.csv')
data = pd.read_csv(data_path)
pd.set_option('display.max_columns', 60)
```

data

```{python}
column_list = data.columns.tolist()
column_list
```

Cleaning

```{python}
clean_data = data.rename(columns={'neighborhood_overview': 'neighbourhood_description'})

```

<!-- #region -->
Stats
Price per night by region
Price per no. properties owned by hosts
Price per review scores



Number of properties owned by owners
A



<!-- #endregion -->

Averages

```{python}
data
```

```{python}

```
