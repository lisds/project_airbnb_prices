""" Project code

This is just an example.

You should document your code, as in the example below, using the Numpy
docstring format (Google it).
"""

def my_func(a, b):
    """ An example function, returning `a` * 10 + b

    Parameters
    ----------
    a : number
    b : number

    Returns
    -------
    res : number
        Result of ``a * 10 + b``.
    """
    return a * 10 + b
